{
  "name": "mon-site-cloudflare",
  "version": "1.0.0",
  "description": "Site Hugo optimisé pour Cloudflare",
  "scripts": {
    "start": "npm install && hugo server",
    "build": "npm install && hugo --minify"
  },
  "dependencies": {
    "autoprefixer": "^10.4.19",
    "postcss-cli": "^11.0.0",
    "tailwindcss": "^3.4.3"
  }
}